{
  "name": "antigravity-web-bridge",
  "version": "1.0.0",
  "description": "Remote control for Antigravity via Firebase",
  "main": "bridge.js",
  "dependencies": {
    "firebase": "^10.8.0",
    "dotenv": "^16.4.5"
  },
  "scripts": {
    "build": "cd web-control && npm install && npm run build",
    "install": "cd web-control && npm install",
    "dev": "cd web-control && npm run dev",
    "start": "cd web-control && npm run start"
  }
}